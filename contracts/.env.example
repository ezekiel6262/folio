# Private key of the deploying wallet. Never commit the real file.
DEPLOYER_PRIVATE_KEY=0x...
# Optional: a private RPC avoids the public endpoint's rate limits.
BASE_RPC_URL=https://mainnet.base.org
# Optional: for `npx hardhat verify`
BASESCAN_API_KEY=
